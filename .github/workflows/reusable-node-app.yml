name: Reusable - Node App
on:
  workflow_call:
    inputs:
      node: { type: string, default: "20" }
    secrets:
      GITHUB_TOKEN: { required: false }

jobs:
  placeholder:
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - run: echo "Node reusable workflow placeholder"
