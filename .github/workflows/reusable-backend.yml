name: Reusable - Backend
on:
  workflow_call:
    inputs:
      java: { type: string, default: "21" }
    secrets:
      GITHUB_TOKEN: { required: true }

jobs:
  placeholder:
    runs-on: ubuntu-latest
    env:
      REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - run: echo "Backend reusable workflow placeholder"
